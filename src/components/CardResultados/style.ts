import { styled } from "styled-components";

export const ContainerCardResultados = styled.div`
    display: flex;
    display: column;
`

export const UFEstado = styled.div`
    display: flex;
    flex-direction: row;
    
`